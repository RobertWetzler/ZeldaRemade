namespace Project.Entities
{
    public enum Facing
    {
        Up,
        Down,
        Left,
        Right
    }
    public enum Move
    {
        Moving,
        Idle,
        UsingSword,
        UsingItem,
        PickUpItem
    }
    public enum LinkColor
    {
        Green,
        Red,
        Blue
    }
}
