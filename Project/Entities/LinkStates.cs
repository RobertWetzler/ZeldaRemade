using System;
using System.Collections.Generic;
using System.Text;

namespace Project.Entities
{
    public enum Facing
    {
        Up,
        Down,
        Left,
        Right
    }
    public enum Move
    {
        Moving,
        Idle,
        UsingSword,
        UsingItem
    }
    public enum LinkColor
    {
        Green,
        Red,
        Blue
    }
}
